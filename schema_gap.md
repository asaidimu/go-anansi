# Current Gap in Schema Migration Implementation

**Problem Context:**
The task involves refactoring the schema to support `FieldSets` while maintaining backward compatibility with `FieldsArray`. This requires modifying 
`core/schema/migration/migrator.go`.
`core/schema/migration/applier.go`.
`core/schema/validator/document.go`.

